# Loki Profile Configuration
# Re-enable interceptor logging so GRAPHQL_REQUEST lines reach Loki
# (either via Promtail scraping stdout or the Loki4j push appender).
# This overrides the OFF level set in application-dev.properties when
# both profiles are active (spring.profiles.active=dev,loki).
logging.level.at.mavila.computing_lab_may_2026.infrastructure.web.graphql.GraphQlRequestLoggingInterceptor=INFO
